# LeafSize

A little utility i made for a certain person.
The person has a scanner with obviouly defined size. The utility loads scan of a leaf, selects a area with a leaf, calculates a single pixel
real size, a leaf's square in pixels and then an actual leaf square. No neural networks and complicated stuff, there is no need for it.
